import React from 'react'

interface Props {
    title: string;
}

const NavButton = ({ title } : Props) => {
  return (
    <button>
        NavButton
    </button>
  )
}

export default NavButton
const styles={
    button:``
}